{% extends 'base.html.twig' %}

{% block title %}{{offer.name}}{% endblock %}

{% block body %}
    <h1 class="text-primary">Edit Offer</h1>

    {{ include('offer/_form.html.twig', {'button_label': 'Update'}) }}

   
    <a class="btn btn-primary" href="{{ path('offer_index') }}">Back</a>

    {{ include('offer/_delete_form.html.twig') }}
{% endblock %}
