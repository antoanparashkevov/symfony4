{% extends 'base.html.twig'%}

{% block title %}{{offer.name}}{% endblock %}

    {# <h1>Offer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ offer.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ offer.slug }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offer.description }}</td>
            </tr>
            <tr>
                <th>Final_price</th>
                <td>{{ offer.finalPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offer_index') }}">back to list</a>

    <a href="{{ path('offer_edit', {'id': offer.id}) }}">edit</a>

    {{ include('offer/_delete_form.html.twig') }} #}

{% block body %}
 <div class="container">
    <div class="row">
        <div class="col-12">
            
            <div class="shadow-sm">
                <div class="container p-4">
                    <div class="row">
                        <div class="col-2 text-center">
                            <img class="mt-4" src="{{asset('images/bricks.png')}}" width="100" height="100">
                        </div>
                        <div class="d-flex flex-column flex-md-column mt-3 ml-5 ml-md-3">
                            <h3 class="title-show">{{ offer.name }}</h3>
                                <p class="d-inline">{{offer.description}}</p>
                                <p><strong>Price: ${{offer.finalPrice}}</strong></p>
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex flex-column p-3 mb-3">
        <a class="btn btn-primary" href="{{ path('offer_index') }}">Back</a>

    <a class="btn btn-secondary" href="{{ path('offer_edit', {'id': offer.id}) }}">Edit</a>

    {{ include('offer/_delete_form.html.twig') }}
    </div>

{% endblock %}
{# <div id='app'></div> #}

{# 
{% block javascripts %}
{{parent()}}

{{encore_entry_script_tags('app')}}
{% endblock %}

{% block stylesheets %}
{{parent()}}

{{encore_entry_link_tags('app')}}
{% endblock %} #}
